BASE="http://localhost:4000"
COOKIE="cookies_assignments.txt"
EMAIL="assignments_test2@example.com"
PASS="pass123"

echo "=== 1) Create account / login ==="
curl -i -c "$COOKIE" -X POST "$BASE/api/auth" \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"$EMAIL\",\"password\":\"$PASS\"}"

echo
echo "=== 2) Create classes ==="
curl -i -b "$COOKIE" -X POST "$BASE/api/classes" \
  -H "Content-Type: application/json" \
  -d '{"className":"CS260","difficulty":"hard"}'

curl -i -b "$COOKIE" -X POST "$BASE/api/classes" \
  -H "Content-Type: application/json" \
  -d '{"className":"MATH101","difficulty":"easy"}'

curl -i -b "$COOKIE" -X POST "$BASE/api/classes" \
  -H "Content-Type: application/json" \
  -d '{"className":"PHYS201","difficulty":"medium"}'

echo
echo "=== 3) Verify classes ==="
curl -i -b "$COOKIE" "$BASE/api/classes"

echo
echo "=== 4) Add assignments to each class ==="
# CS260 assignments
curl -i -b "$COOKIE" -X POST "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"CS260 Lab 1","className":"CS260","dueDate":"2026-03-31","difficulty":"medium"}'

curl -i -b "$COOKIE" -X POST "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"CS260 Project","className":"CS260","dueDate":"2026-04-10","difficulty":"hard"}'

# MATH101 assignments
curl -i -b "$COOKIE" -X POST "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"MATH101 HW 1","className":"MATH101","dueDate":"2026-03-25","difficulty":"easy"}'

curl -i -b "$COOKIE" -X POST "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"MATH101 Quiz Prep","className":"MATH101","dueDate":"2026-03-28","difficulty":"medium"}'

# PHYS201 assignments
curl -i -b "$COOKIE" -X POST "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"PHYS201 Problem Set 1","className":"PHYS201","dueDate":"2026-04-02","difficulty":"medium"}'

curl -i -b "$COOKIE" -X POST "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"PHYS201 Lab Report","className":"PHYS201","dueDate":"2026-04-06","difficulty":"hard"}'

echo
echo "=== 5) Verify assignments before deletes ==="
curl -i -b "$COOKIE" "$BASE/api/assignments"

echo
echo "=== 6) Delete a few specific assignments ==="
curl -i -b "$COOKIE" -X DELETE "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"CS260 Lab 1","className":"CS260"}'

curl -i -b "$COOKIE" -X DELETE "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"MATH101 Quiz Prep","className":"MATH101"}'

curl -i -b "$COOKIE" -X DELETE "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"PHYS201 Lab Report","className":"PHYS201"}'

echo
echo "=== 7) Verify assignments after deletes ==="
curl -i -b "$COOKIE" "$BASE/api/assignments"

echo
echo "=== 8) Optional negative test: delete non-existent assignment (should fail or no-op based on server logic) ==="
curl -i -b "$COOKIE" -X DELETE "$BASE/api/assignments" \
  -H "Content-Type: application/json" \
  -d '{"assignmentName":"DOES_NOT_EXIST","className":"CS260"}'
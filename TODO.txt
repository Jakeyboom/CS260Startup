Here is a list of things to do for the next part of the projects.

1. Stub out endpoints to be implemented for full functionality.
    REGISTRATION (returns a cookie or nothing if 409)
    1. POST /api/auth HTTP/2
       Content-Type: application/json
       {
        "email":"example@example.com",
        "password":"password"
       }

       (Response)
       HTTP/2 200 OK
       Content-Type: application/json
       Set-Cookie: auth=tokenHere

       {
        "email":"example@example.com"
       }

    LOGIN (returns a cookie or nothing if 401)
        PUT /api/auth HTTP/2
        Content-Type: application/json
        {
            "email":"marta@id.com",
            "password":"toomanysecrets"
        }

        HTTP/2 200 OK
        Content-Type: application/json
        Set-Cookie: auth=tokenHere
    {
      "email":"marta@id.com"
    }